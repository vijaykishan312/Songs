import React from 'react';
import type { IWaveformCandle } from './WaveformCandleTypes';
export declare const WaveformCandle: ({ index, amplitude, parentViewLayout, candleWidth, candleSpace, noOfSamples, songDuration, currentProgress, waveColor, scrubColor, candleHeightScale }: IWaveformCandle) => React.JSX.Element;
